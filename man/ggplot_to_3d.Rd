% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ggplot3d.R
\name{ggplot_to_3d}
\alias{ggplot_to_3d}
\title{Convert a ggplot2 plot to a 3D model via Tripo}
\usage{
ggplot_to_3d(
  p,
  width = 1200,
  height = 800,
  dpi = 150,
  wait = TRUE,
  via_llm = TRUE,
  model = NULL,
  ...
)
}
\arguments{
\item{p}{A ggplot object.}

\item{width}{Image width in pixels. Default 1200.}

\item{height}{Image height in pixels. Default 800.}

\item{dpi}{Resolution in dots per inch. Default 150.}

\item{wait}{If \code{TRUE} (default), poll until completion and return a
\code{cast3d_model}. If \code{FALSE}, return a \code{cast3d_task}.}

\item{via_llm}{If \code{TRUE}, route through \code{generate_3d_via_llm()} with
\code{mode = "data_viz"} instead of direct \code{image_to_model}. Requires
aisdk. Default \code{TRUE} (data visualizations benefit from LLM routing).}

\item{model}{Optional LLM model passed to \code{describe_image_for_3d()}
when \code{via_llm = TRUE}.}

\item{...}{Additional arguments passed to \code{generate_3d()} (direct mode)
or \code{generate_3d_via_llm()} (LLM mode).}
}
\value{
A \code{cast3d_model} if \code{wait = TRUE}, otherwise a \code{cast3d_task}.
}
\description{
Renders a ggplot object as a high-resolution PNG, then submits it
to Tripo's \code{image_to_model} pipeline. The resulting 3D model can be
interactively viewed with \code{view_3d()}.
}
\details{
Direct \code{image_to_model} works well for photographs and objects but
may produce poor results for data visualizations. Set \code{via_llm = TRUE}
to route through a vision LLM that translates the plot into a
data-sculpture description optimized for text-to-3D generation.
}
\examples{
\dontrun{
library(ggplot2)
p <- ggplot(mtcars, aes(wt, mpg, size = hp, color = cyl)) +
  geom_point() +
  theme_minimal()

model <- ggplot_to_3d(p)
view_3d(model)

model_llm <- ggplot_to_3d(p, via_llm = TRUE)
view_3d(model_llm)
}
}
