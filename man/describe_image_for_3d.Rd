% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/llm.R
\name{describe_image_for_3d}
\alias{describe_image_for_3d}
\title{Describe an image for 3D generation via LLM}
\usage{
describe_image_for_3d(
  image,
  model = NULL,
  style = NULL,
  mode = c("auto", "object", "data_viz"),
  ...
)
}
\arguments{
\item{image}{Path to a local image file or a URL.}

\item{model}{A language model object from aisdk (e.g.
\code{aisdk::create_openai()$language_model("gpt-4o")}), or a
\code{"provider:model"} string like \code{"openai:gpt-4o"}. Defaults to the
aisdk package-wide default model (see \code{aisdk::set_model()}),
falling back to \code{"openai:gpt-4o"}.}

\item{style}{Optional Tripo style hint (e.g. \code{"person:person2cartoon"},
\code{"object:clay"}, \code{"gold"}).}

\item{mode}{Description mode. \code{"auto"} (default) auto-detects from image
content; \code{"object"} for photographs or natural objects; \code{"data_viz"}
for statistical charts, plots, heatmaps, and other data visualizations —
this produces data-sculpture-style descriptions optimized for text-to-3D.}

\item{...}{Additional arguments passed to \code{aisdk::analyze_image()}.}
}
\value{
A character string with a 3D-optimized description.
}
\description{
Uses a vision-capable LLM (via aisdk) to analyze an image and produce
a structured description optimized as input for Tripo's \code{text_to_model}
endpoint. This can produce better 3D results than direct \code{image_to_model}
when the image contains complex or subtle details.
}
\examples{
\dontrun{
library(aisdk)
desc <- describe_image_for_3d("photo.jpg")
cat(desc)

desc_viz <- describe_image_for_3d("umap_plot.png", mode = "data_viz")
cat(desc_viz)
}
}
