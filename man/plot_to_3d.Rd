% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ggplot3d.R
\name{plot_to_3d}
\alias{plot_to_3d}
\title{Convert a plot expression to a 3D model via Tripo}
\usage{
plot_to_3d(
  expr,
  width = 1200,
  height = 800,
  dpi = 150,
  wait = TRUE,
  via_llm = TRUE,
  model = NULL,
  ...
)
}
\arguments{
\item{expr}{An expression that produces a plot (e.g. \code{plot(cars)}).}

\item{width}{Image width in pixels. Default 1200.}

\item{height}{Image height in pixels. Default 800.}

\item{dpi}{Resolution in dots per inch. Default 150.}

\item{wait}{If \code{TRUE} (default), poll until completion.}

\item{via_llm}{If \code{TRUE}, route through \code{generate_3d_via_llm()} with
\code{mode = "data_viz"} instead of direct \code{image_to_model}. Default \code{TRUE}.}

\item{model}{Optional LLM model when \code{via_llm = TRUE}.}

\item{...}{Additional arguments passed to \code{generate_3d()} (direct mode)
or \code{generate_3d_via_llm()} (LLM mode).}
}
\value{
A \code{cast3d_model} if \code{wait = TRUE}, otherwise a \code{cast3d_task}.
}
\description{
Evaluates a plot expression, captures its output as a PNG, and
generates a 3D model. Works with both base R graphics and grid-based
plots.
}
\details{
Direct \code{image_to_model} works well for photographs and objects but
may produce poor results for data visualizations. Set \code{via_llm = TRUE}
to route through a vision LLM.
}
\examples{
\dontrun{
model <- plot_to_3d({
  plot(mtcars$wt, mtcars$mpg,
       pch = 19, col = "steelblue",
       xlab = "Weight", ylab = "MPG")
})
view_3d(model)
}
}
